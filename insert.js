const dbconnect = require('./mongodb')


const insert= async ()=>{
    // const db = await dbconnect();
    // const result = await db.insertOne(
    //     {name : "shyam", isVerified : false}

    // );
    // if(result.acknowledged){
    //     console.log("data inserted")
    // }
}
insert();